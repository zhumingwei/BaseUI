package com.bond.baseui.network.image

import android.content.Context
import com.bond.baseui.network.image.constant.Constants

class ImageLoader {
    // todo
    val TAG = "ImageLoader"
    fun init(context: Context) {
        Constants.init(context)
    }

}